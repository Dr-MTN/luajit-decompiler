# Will automatically be changed if file is listed below
use_version = 2.1

# Add fully-qualified names and versions of lua files to relevant lists below.
# Lists are 'profiles' for a set of files to decompile.
version_default = {
    # "test/item_trinket_list.lua": 2.0
}

_LUA_FILE_VERSIONS = {"version_default": version_default}
